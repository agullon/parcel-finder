---
apiVersion: v1
kind: Service
metadata:
  name: parcel-finder-service
spec:
  selector:
    platform: telegram
    name: parcel-finder
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
